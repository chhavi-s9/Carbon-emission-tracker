Place your two images here:
1. hero_hand_tree.jpg  <- the hand-touching-tree photo (used as hero image, Home page, right side)
2. logo.png            <- the green carbon-footprint icon (used as favicon + header logo)
